package Interface.Rules;

public interface Prey 
{   /*
        This method is missing public because
        every method in the interface is public and defaul

        you can have completed methods, but they would 
        have to be private, which makes sense
     */
    void flee();
}
